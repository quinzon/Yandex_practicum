name: Python CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.10.12, 3.11, 3.12]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install wemake-python-styleguide flake8 flake8-html mypy --verbose

      - name: Create report directory
        run: |
          mkdir -p flake-report

      - name: Run flake8 and generate HTML report
        id: flake8_check
        run: |
          flake8 . --config=setup.cfg --format=html --htmldir=flake-report && echo "flake8_success=true" >> $GITHUB_ENV || echo "flake8 failed"

      - name: Upload HTML report as artifact
        uses: actions/upload-artifact@v4
        with:
          name: flake-report-${{ matrix.python-version }}
          path: flake-report/

      - name: Run mypy type check
        id: mypy_check
        run: |
          mypy . && echo "mypy_success=true" >> $GITHUB_ENV || echo "mypy failed"

      - name: Send Telegram notification
        if: always()
        run: |
          MESSAGE="Python CI for version ${{ matrix.python-version }}:\n\n"
          
          if [[ "${{ env.flake8_success }}" == "true" ]]; then
              MESSAGE+="flake8 - success\n"
          else
              MESSAGE+="flake8 - failed\n"
          fi
          
          if [[ "${{ env.mypy_success }}" == "true" ]]; then
              MESSAGE+="mypy - success\n"
          else
              MESSAGE+="mypy - failed\n"
          fi

          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
               -d chat_id=${{ secrets.TELEGRAM_BOT_CHAT_ID }} \
               -d text="$(echo -e "$MESSAGE")" \
               -d parse_mode="Markdown"

      - name: Exit with code from previous checks
        run: |
          if [[ "${{ env.flake8_success }}" == "true" && "${{ env.mypy_success }}" == "true" ]]; then
            exit 0
          else
            exit 1
          fi
